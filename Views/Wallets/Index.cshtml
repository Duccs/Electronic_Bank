@model IEnumerable<Electronic_Bank.Models.Wallet>

@{
    ViewData["Title"] = "Wallets";
}

<div class="col-lg-12 grid-margin stretch-card">
<div class="card">
    <div class="card-body">
        <h4 class="display-2">Wallets</h4>
        <br />
        <p>
            <a class="btn btn-secondary btn-rounded btn-fw" asp-action="Create">Create New</a>
            <form asp-controller="Wallets" asp-action="Index" method="get" class="d-flex">
            <p>
                <input type="text" name="searchString" placeholder="Search Client" class="form-control me-sm-2"/>
                <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
            </p>
            </form>
        </p>

        <table class="table table-hover">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Client)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Amount)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Currency)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
        @foreach (var item in Model) {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Client.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Amount)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Currency.Code)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id"><i class="fas fa-edit"></i></a> |
                        <a asp-action="Details" asp-route-id="@item.Id"><i class="fas fa-info-circle"></i></a> |
                        <a asp-action="Delete" asp-route-id="@item.Id"><i class="fas fa-trash"></i></a>
                    </td>
                </tr>
        }
            </tbody>
        </table>
    </div>
</div>
</div>
